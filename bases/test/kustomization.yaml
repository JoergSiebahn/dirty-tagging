apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: test
    literals:
      - foo=bar
      - bar=foo
images:
  - name: localhost/some-image
    newName: public.host/some-image
    newTag: ${TAG_NAME}
