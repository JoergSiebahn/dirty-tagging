apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: test
    literals:
      - foo=bar
images:
  - name: localhost/some-image
    newName: public.host/some-image
    newTag: 1.2.3
